# 视觉巡线与路口识别机器人系统

> 支持白线巡线、路口交点识别与动作组联动控制。

## 一、项目简介
一个基于 OpenCV 与机器人动作组控制的视觉导航系统。项目通过摄像头实时获取画面，对地面白线进行分区加权跟踪，并结合霍夫直线与交点计算实现路口检测，最终驱动机器人完成前进、微调转向与路口通过。

## 二、核心功能
- 白线巡线：基于 LAB 色彩空间阈值分割目标线条
- 分区加权定位：将图像划分为上/中/下三段并加权计算中心
- 路口识别：通过边缘检测 + 概率霍夫直线检测候选线段
- 交点计算：对线段进行两两求交，识别有效路口点
- 动作联动：根据偏移量触发前进、左转、右转动作组
## 三、技术方案

### 1）视觉感知

- 图像预处理：缩放、Gaussian Blur、形态学腐蚀/膨胀
- 颜色分割：LAB 阈值（默认白色）
- 轮廓提取：寻找最大有效轮廓并计算最小外接矩形
- 中心估计：多 ROI 中心点按权重融合得到最终巡线中心

### 2）路径决策

- 横向误差：使用 `line_center_x - img_centerx` 作为控制依据
- 控制策略：
  - 偏差过大向右：执行 `turn_right_small_step`
  - 偏差过大向左：执行 `turn_left_small_step`
  - 偏差在可接受范围：执行 `go_forward_fast`

### 3）路口检测

- 使用 Canny 边缘提取有效边缘
- 使用 `HoughLinesP` 检测线段
- 通过斜率差与线段参数方程计算交点
- 检测到交点后触发直行/通过动作（适赛化改进，用于机器人冲线）

## 四、运行环境

- Python 3
- OpenCV (`cv2`)
- NumPy


## 五、优化方向

- 引入 PID 控制替代阈值式转向策略
- 增加多颜色/多车道适配能力
- 对交点检测加入时序滤波，降低误检
- 解耦硬件接口并补充单元测试，提升可移植性


